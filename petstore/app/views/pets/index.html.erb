<h1>Listing pets</h1>
<div id="testjs">
  off
</div>
<%#= link_to_function "test js", '$(this).html("js works")'%>
<table>
  <tr>
    <th>Name</th>
    <th>Species</th>
    <th>Age</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pets.each do |pet| %>
  <tr>
    <td><%= pet.name %></td>
    <td><%= pet.species %></td>
    <td><%= pet.age %></td>
    <td><%= link_to 'Show', pet %></td>
    <td><%= link_to 'Edit', edit_pet_path(pet) %></td>
    <td><%= link_to 'Destroy', pet, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pet', new_pet_path %>
